# abort script on any command that exits with a non zero value
set -e -x

VERSION=1.1.0-rc1-c1.1.0rc1

echo "Extracting Influx Data Node..."
tar -xvf influxdb/influxdb-meta-${VERSION}_linux_amd64.tar.gz

echo "Preparing Influx Data Node..."
cp -a influxdb-meta-${VERSION}-1/. ${BOSH_INSTALL_TARGET}
